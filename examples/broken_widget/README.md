# Sample: widget-cli boundary failure

Fabricated session. Not a real product history.

The raw transcript is `transcript.md`. Run:

```bash
afr ingest examples/broken_widget/transcript.md --print
```

Story:

1. Agent starts implementing `--json`.
2. Notices thousands of untracked files.
3. Freezes writes.
4. Distinguishes a separate directory from a separate Git repository.
5. Probes four candidate roots with the same tests.
6. Compares against the installed v1 baseline using project-policy excludes.
7. Preserves the accidental build.
8. Names a bounded repair target.
9. Resumes, tests, verifies.

A second session in `transcript_second_session.md` exists so the pattern library can accumulate evidence from more than one model.
