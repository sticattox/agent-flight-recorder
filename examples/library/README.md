# Example library

`widget-library.json` is produced by:

```bash
afr library examples/broken_widget/transcript.md \
            examples/broken_widget/transcript_second_session.md \
            -o examples/library/widget-library.json
```

Two fabricated sessions, two models, same project. AFR-P001 should show more than one evidence session.
